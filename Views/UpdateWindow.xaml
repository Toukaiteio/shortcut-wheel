<Window x:Class="ShortcutWheel.Views.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ShortcutWheel - 检查更新 / Check for Updates"
        Width="600" Height="500"
        Background="#1A1612"
        Foreground="#F2ECDD"
        FontFamily="Microsoft YaHei UI, Segoe UI"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True">

    <Window.Resources>
        <SolidColorBrush x:Key="Bg.Panel" Color="#241E18" />
        <SolidColorBrush x:Key="Bg.Hover" Color="#3A2F22" />
        <SolidColorBrush x:Key="Border.Subtle" Color="#3A332A" />
        <SolidColorBrush x:Key="Trim.Gold" Color="#C9A24E" />
        <SolidColorBrush x:Key="Trim.GoldDim" Color="#7A6A48" />
        <SolidColorBrush x:Key="Text.Primary" Color="#F2ECDD" />
        <SolidColorBrush x:Key="Text.Secondary" Color="#9A8E78" />

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Bg.Panel}" />
            <Setter Property="Foreground" Value="{StaticResource Text.Primary}" />
            <Setter Property="BorderBrush" Value="{StaticResource Trim.GoldDim}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,7" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Bg.Hover}" />
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Trim.Gold}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ProgressBar">
            <Setter Property="Background" Value="{StaticResource Bg.Panel}" />
            <Setter Property="Foreground" Value="{StaticResource Trim.Gold}" />
            <Setter Property="BorderBrush" Value="{StaticResource Border.Subtle}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="✦ 发现新版本 / Update Available"
                       FontSize="20" FontWeight="Bold"
                       Foreground="{StaticResource Trim.Gold}" />
        </StackPanel>

        <!-- Version line -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,14">
            <TextBlock Text="当前 / Current: " Foreground="{StaticResource Text.Secondary}" FontSize="12" />
            <TextBlock x:Name="CurrentVersionText" Foreground="{StaticResource Text.Primary}"
                       FontSize="12" Margin="2,0,12,0" />
            <TextBlock Text="→" Foreground="{StaticResource Trim.Gold}" Margin="0,0,12,0" />
            <TextBlock Text="最新 / Latest: " Foreground="{StaticResource Text.Secondary}" FontSize="12" />
            <TextBlock x:Name="NewVersionText" Foreground="{StaticResource Trim.Gold}"
                       FontWeight="Bold" FontSize="12" Margin="2,0,0,0" />
        </StackPanel>

        <!-- Release notes -->
        <Border Grid.Row="2"
                Background="{StaticResource Bg.Panel}"
                BorderBrush="{StaticResource Border.Subtle}"
                BorderThickness="1" CornerRadius="3">
            <ScrollViewer Padding="14" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="NotesText"
                           TextWrapping="Wrap"
                           Foreground="{StaticResource Text.Primary}"
                           FontSize="12"
                           LineHeight="18"
                           FontFamily="Consolas, Microsoft YaHei UI" />
            </ScrollViewer>
        </Border>

        <!-- Progress -->
        <StackPanel Grid.Row="3" Margin="0,14,0,0">
            <ProgressBar x:Name="DownloadProgress" Height="6" Visibility="Collapsed" />
            <TextBlock x:Name="StatusText" Margin="0,6,0,0"
                       Foreground="{StaticResource Text.Secondary}" FontSize="11" />
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button x:Name="BtnViewOnGitHub" Content="查看 / View" Width="100" Margin="0,0,8,0"
                    Click="BtnViewOnGitHub_Click" />
            <Button x:Name="BtnLater" Content="稍后 / Later" Width="100" Margin="0,0,8,0"
                    Click="BtnLater_Click" />
            <Button x:Name="BtnInstall" Content="下载并安装 / Install" Width="160"
                    Click="BtnInstall_Click">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Setter Property="Background" Value="{StaticResource Trim.Gold}" />
                        <Setter Property="Foreground" Value="#1A1612" />
                        <Setter Property="FontWeight" Value="Bold" />
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
